[tool.poetry]
name = "audio-optimizer-bot"
version = "1.0.0"
description = "Music and audio optimization bot"

[tool.poetry.dependencies]
python = "^3.11"
discord.py = "^2.3.2"
aiohttp = "^3.9.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"